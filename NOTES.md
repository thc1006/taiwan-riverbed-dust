# 補充說明 · Notes

[README](README.md) 的延伸：紀律聲明、其餘安慰劑、下風判定與資料取用。

## 紀律聲明

分析腳本在看到任何結果之前就寫好，包含三道防線。資料抓完後沒有修改門檻、沒有更換指標、沒有
挑選子樣本。敏感度表把 5 組距離、角度門檻全部攤開，結論一致。

## 其餘假來源

南移 25 km 為 +0.00（p = 0.98）、山區不顯著、海上無下風樣本。只有北移顯著，完全符合「東北季風
把北方上游汙染帶下來」的解釋。

## 下風判定

把「來源到測站的方位角」對上「區域向量平均風的吹向」，容差 ±45°、距離 ≤ 40 km。

## 資料取用

現行金鑰由環境部公告在 [data.gov.tw dataset 40448](https://data.gov.tw/api/v2/rest/dataset/40448)
的 `resourceDownloadUrl` 欄位；正式使用建議自行到平台免費註冊。伺服器端日期篩選不可靠（`filters`
與 `year_month` 都回當日資料），歷史用 `offset` 分頁，多年份走 airtw 分年封存。回應最外層是 list。
首次抓約 14 萬筆逐時資料（142 次請求，逐批落地、可中斷續抓）。
